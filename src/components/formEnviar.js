import { useLocation } from "react-router-dom";

const FormEnviar = () => {
    const { state } = useLocation();
    const { emisor } = state;

    return (
        <>
        </>
    )
}

export default FormEnviar